module.exports = {

	// Path where this instance of the Functio UI is hosted (string)
	clientPath: '/',
	// Port where the dev server runs (int)
	devPort: 8000,
	// URL or path of the Functio document to start with (string)
	document: 'processes.json'

};